# Learning Note

このノートは、3か月後の自分が読み返しても分かるように、アプリ全体の流れを初心者向けに整理したものです。

## 新しい設計方針

このアプリでは、PDFとAPIの役割を分けます。

PDFは、会社の説明、事業の方向性、リスク、経営陣のコメントなどの定性情報を読むために使います。

株価、EPS、PER、PBR、時価総額などの投資指標は、APIから取得する前提にします。

PDFから抽出した売上・利益・EPSは誤検知することがあるため、正式データとして表示せず、PER計算にも使いません。

## システム全体の流れ

```text
決算PDF
  ↓
pdf_reader.py でテキスト抽出
  ↓
numeric_extractor.py でPDF由来の参考情報セクションを作成

証券コード
  ↓
market_data_client.py でAPI由来データを取得
  ↓
valuation_analyzer.py でAPI由来EPSだけを使ってPER分析

PDFテキスト + API由来データ
  ↓
ai_analyzer.py でAI要約
  ↓
report_writer.py でMarkdown保存
```

## 各ファイルの役割

### main.py

アプリの入り口です。

PDF読み込み、PDF参考情報抽出、APIデータ取得、AI要約、投資判断支援セクション作成、Markdown保存を順番に実行します。

### src/pdf_reader.py

PDFからテキストを抽出します。

このテキストは主に定性情報の分析に使います。

### src/numeric_extractor.py

PDFから数値らしき情報を抽出します。

ただし、売上、利益、EPSの抽出結果は正式データとして表示しません。

PDF抽出値は、脚注、注記、業績予想レンジなどを誤って拾う可能性があるため、PER計算などには使いません。

### src/market_data_client.py

API由来データを取得します。

現在は試作用としてyfinanceを使います。

将来的には、このファイルをJ-Quants APIへ差し替える想定です。

### src/valuation_analyzer.py

PERや割安度分析を担当します。

実績PERは、API由来の現在株価とAPI由来のEPSが両方取得できた場合だけ計算します。

PDF由来のEPSは使いません。

### src/ai_analyzer.py

AI要約を担当します。

AIはPDFから定性情報を整理します。

売上、営業利益、純利益、EPS、株価、PER、PBR、時価総額などの正式データはAPI由来データだけを使い、推測しません。

### src/data_models.py

PDF由来データ、API由来データ、PER分析などのデータ構造を定義します。

### src/report_writer.py

Markdownレポートを保存します。

## main.pyを実行した時の処理順序

```powershell
python main.py data/pdfs/sample.pdf --ticker 285A
```

このとき、処理は次の順番で進みます。

1. `main.py` が起動する
2. PDFパスと証券コードを受け取る
3. `.env` からOpenAI APIキーを読み込む
4. `pdf_reader.py` がPDFテキストを抽出する
5. `numeric_extractor.py` がPDF由来の参考情報セクションを作る
6. `market_data_client.py` がAPI由来データを取得する
7. `ai_analyzer.py` がPDFテキストとAPI由来データを使って要約する
8. `valuation_analyzer.py` がAPI由来EPSだけを使ってPER分析する
9. `main.py` がPDF由来、API由来、AI要約、投資判断支援を結合する
10. `report_writer.py` がMarkdownファイルを保存する

## なぜPDF数値をPER計算に使わないのか

PDFから数値を抽出するとき、表の構造、脚注、注記、予想レンジなどが混ざることがあります。

たとえば、EPSではなく脚注番号の `3` を拾ってしまうような誤検知が起きます。

投資判断では、PERのような指標が間違うと判断全体が崩れます。

そのため、PDF由来の売上・利益・EPSは正式データとして表示せず、投資指標計算にはAPI由来データだけを使います。

## なぜPDF由来の定量表現を断定しないのか

PDFには、売上、営業利益、純利益、EPS、前年比、D/Eレシオなどの数字がたくさんあります。

しかし、それらは通期、四半期、Non-GAAP、IFRS、業績予想、注記など、意味が違う数字として並んでいることがあります。

AIがPDFだけを読んで「過去最高」「約2倍」「大幅増加」「利益率改善」と断定すると、どの数字を根拠にしたのか分かりにくくなります。

そのため、このアプリではAPI由来データが未取得の項目について、AIに定量評価を断定させません。

PDF内の記述に触れる場合は、次のように表現します。

```text
PDF上の記述では、利益改善が示唆されています。
```

次のような断定は避けます。

```text
営業利益が過去最高です。
前年比で約2倍です。
利益率が改善しています。
```

この方針により、初見のPDFでもAIが数字を過信しにくいレポートになります。

## なぜ情報源を分けてMarkdownに出すのか

情報源を分けると、どの数字がどこから来たのか確認しやすくなります。

- PDF由来: 決算資料を定性情報として読むための参考情報
- API由来: 株価やEPSなど、計算に使うデータ
- AI要約: PDFの定性情報とAPI由来データを分けた解釈

この分離により、AIが数字をそれっぽく作るリスクを減らせます。

## なぜ責務分離したのか

責務分離とは、役割ごとにファイルを分けることです。

このアプリでは、PDF読み込み、API取得、AI要約、PER分析、保存処理を分けています。

こうすると、将来J-Quants APIへ差し替えるときも、主に `market_data_client.py` を直せば済みます。

## このVerでまだやらないこと

自動売買はまだ実装しません。

理由は、売買機能はお金に直接関わるため、データ取得、検証、ログ保存、失敗時の安全設計などを先に作る必要があるからです。
