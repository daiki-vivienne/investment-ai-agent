# Learning Note

このノートは、3か月後の自分が読み返しても分かるように、アプリ全体の流れを初心者向けに整理したものです。

## 新しい設計方針

このアプリでは、PDFとAPIの役割を分けます。

PDFは、会社の説明、事業の方向性、リスク、経営陣のコメントなどの定性情報を読むために使います。

株価、EPS、PER、PBR、時価総額などの投資指標は、APIから取得する前提にします。

PDFから抽出した売上・利益・EPSは誤検知することがあるため、正式データとして表示せず、PER計算にも使いません。

## システム全体の流れ

```text
決算PDF
  ↓
pdf_reader.py でテキスト抽出
  ↓
numeric_extractor.py でPDF由来の参考情報セクションを作成

証券コード
  ↓
market_data_client.py でAPI由来データを取得
  ↓
valuation_analyzer.py でAPI由来EPSだけを使ってPER分析

PDFテキスト + API由来データ
  ↓
ai_analyzer.py でAI要約
  ↓
report_writer.py でMarkdown保存
```

## 各ファイルの役割

### main.py

アプリの入り口です。

PDF読み込み、PDF参考情報抽出、APIデータ取得、AI要約、投資判断支援セクション作成、Markdown保存を順番に実行します。

### src/pdf_reader.py

PDFからテキストを抽出します。

このテキストは主に定性情報の分析に使います。

### src/numeric_extractor.py

PDFから数値らしき情報を抽出します。

ただし、売上、利益、EPSの抽出結果は正式データとして表示しません。

PDF抽出値は、脚注、注記、業績予想レンジなどを誤って拾う可能性があるため、PER計算などには使いません。

### src/market_data_client.py

API由来データを取得します。

現在は、株価、PBR、時価総額などの参考市場データにyfinanceを使います。

売上、営業利益、純利益、EPSなどの正式財務データは、期間一致を確認したJ-Quantsデータを使います。

将来的には、市場データ側もJ-Quantsや他の正式データソースで検証できるように拡張します。

### src/valuation_analyzer.py

PERや割安度分析を担当します。

実績PERは、API由来の現在株価とAPI由来のEPSが両方取得できた場合だけ計算します。

PDF由来のEPSは使いません。

### src/ai_analyzer.py

AI要約を担当します。

AIはPDFから定性情報を整理します。

売上、営業利益、純利益、EPS、株価、PER、PBR、時価総額などの正式データはAPI由来データだけを使い、推測しません。

### src/data_models.py

PDF由来データ、API由来データ、PER分析などのデータ構造を定義します。

### src/report_writer.py

Markdownレポートを保存します。

## main.pyを実行した時の処理順序

```powershell
python main.py data/pdfs/sample.pdf --ticker 285A
```

このとき、処理は次の順番で進みます。

1. `main.py` が起動する
2. PDFパスと証券コードを受け取る
3. `.env` からOpenAI APIキーを読み込む
4. `pdf_reader.py` がPDFテキストを抽出する
5. `numeric_extractor.py` がPDF由来の参考情報セクションを作る
6. `market_data_client.py` がAPI由来データを取得する
7. `ai_analyzer.py` がPDFテキストとAPI由来データを使って要約する
8. `valuation_analyzer.py` がAPI由来EPSだけを使ってPER分析する
9. `main.py` がPDF由来、API由来、AI要約、投資判断支援を結合する
10. `report_writer.py` がMarkdownファイルを保存する

## なぜPDF数値をPER計算に使わないのか

PDFから数値を抽出するとき、表の構造、脚注、注記、予想レンジなどが混ざることがあります。

たとえば、EPSではなく脚注番号の `3` を拾ってしまうような誤検知が起きます。

投資判断では、PERのような指標が間違うと判断全体が崩れます。

そのため、PDF由来の売上・利益・EPSは正式データとして表示せず、投資指標計算にはAPI由来データだけを使います。

## なぜPDF由来の定量表現を断定しないのか

PDFには、売上、営業利益、純利益、EPS、前年比、D/Eレシオなどの数字がたくさんあります。

しかし、それらは通期、四半期、Non-GAAP、IFRS、業績予想、注記など、意味が違う数字として並んでいることがあります。

AIがPDFだけを読んで「過去最高」「約2倍」「大幅増加」「利益率改善」と断定すると、どの数字を根拠にしたのか分かりにくくなります。

そのため、このアプリではAPI由来データが未取得の項目について、AIに定量評価を断定させません。

PDF内の記述に触れる場合は、次のように表現します。

```text
PDF上の記述では、利益改善が示唆されています。
```

次のような断定は避けます。

```text
営業利益が過去最高です。
前年比で約2倍です。
利益率が改善しています。
```

この方針により、初見のPDFでもAIが数字を過信しにくいレポートになります。

## なぜ情報源を分けてMarkdownに出すのか

情報源を分けると、どの数字がどこから来たのか確認しやすくなります。

- PDF由来: 決算資料を定性情報として読むための参考情報
- API由来: 株価やEPSなど、計算に使うデータ
- AI要約: PDFの定性情報とAPI由来データを分けた解釈

この分離により、AIが数字をそれっぽく作るリスクを減らせます。

## なぜ責務分離したのか

責務分離とは、役割ごとにファイルを分けることです。

このアプリでは、PDF読み込み、API取得、AI要約、PER分析、保存処理を分けています。

こうすると、将来J-Quants APIへ差し替えるときも、主に `market_data_client.py` を直せば済みます。

## このVerでまだやらないこと

自動売買はまだ実装しません。

理由は、売買機能はお金に直接関わるため、PDF分析、データ取得、スコアリング、検証、ログ保存、失敗時の安全設計などを先に作る必要があるからです。

## Ver3.0で追加した考え方

Ver3.0では、PDFとAPIの期間が一致しているかを確認する仕組みを追加しました。

投資分析では、売上や利益の数字そのものだけでなく、「その数字がどの決算期のものか」がとても重要です。

たとえば、PDFが2026年3月期の通期決算なのに、APIから取れた数字が2026年3月期の3Q累計だった場合、その2つを同じものとして比較すると間違った分析になります。

### なぜPDFから決算期を推測しないのか

PDFには、通期実績、四半期実績、前年同期比、業績予想、脚注など、たくさんの数字が書かれています。

AIや単純な文字検索で決算期を推測すると、業績予想や脚注の数字を実績と勘違いする可能性があります。

そのため、Ver3.0ではPDFから決算期を推測しません。

ユーザーがCLIで指定した `--period-type` と `--fiscal-year-end` を正しい条件として扱います。

### Ver3.0の処理の流れ

```text
決算PDF
  ↓
PDFテキスト抽出
  ↓
ユーザー指定の決算期を確認
  ↓
J-Quantsで同じ期間のstatementを検索
  ↓
期間が一致した場合だけ正式データ候補にする
  ↓
期間が一致しないデータは参考データとして表示する
  ↓
AI分析
  ↓
期間整合性チェック付きMarkdownを保存
```

### 追加されたファイルの役割

### src/jquants_client.py

J-Quants APIから財務サマリーを取得し、ユーザー指定の決算期と一致するstatementを探します。

期間が一致しない場合は、正式データとして扱わず「未取得」にします。

### src/api_report_formatter.py

J-Quantsの確認結果をMarkdownに変換します。

「期間整合性チェック」「正式データ候補」「参考データ一覧」を作る役割です。

### なぜ参考データを分析に使わないのか

参考データは、存在を確認するためには便利です。

しかし、期間が違う数字を使って「利益が増えた」「PERが高い/低い」と判断すると、投資判断を誤る可能性があります。

そのため、Ver3.0では期間が一致しないAPIデータを分析やPER計算に使いません。

### なぜAIスコアも未取得にするのか

AIスコアは便利ですが、正式データがない状態で点数を出すと、PDF由来の参考数値を正式な根拠のように見せてしまう危険があります。

Ver3.0では、ユーザー指定期間と一致するAPI正式データが未取得の場合、AIスコアも「未取得」とします。

これは分析を弱くするためではなく、誤った自信を持たせないための安全設計です。

### なぜPDFの「過去最高」も分析根拠にしないのか

PDF上に「過去最高」や「大幅増加」と書かれていても、それがAPI正式データと同じ期間・同じ会計基準・同じ指標定義とは限りません。

そのためVer3.0では、API正式データが未取得の場合、PDF上の定量表現は「参考情報」として紹介するだけにします。

AI本文のポジティブ要因や投資メモでは、PDF由来の数値を根拠にした断定評価を避けます。

さらに、Ver3.0ではAPI正式データが未取得の場合、AIにPDF内の具体的な数値を書き写させません。

理由は、AIがPDF内の売上・営業利益・純利益・前年比の対応関係を読み間違える可能性があるためです。

数字を正確に扱う必要がある場合は、期間が一致したAPI正式データを使います。

### なぜ業績サマリーをAIに書かせないのか

AIに売上、営業利益、純利益、EPSを書かせると、PDF由来の数値や別定義の数値と混ざる可能性があります。

そのためVer3.0では、API正式業績サマリーをJ-Quants正式データからコードで機械的に生成します。

AI本文では、正式データの数値を再解釈させません。

### なぜAI出力後にも安全フィルタをかけるのか

プロンプトで注意しても、AIがPDF由来の「過去最高」「キャッシュフロー」「借入金」などを本文やスコア理由に混ぜてしまうことがあります。

そのためVer3.0では、AI出力後にも危険な表現をチェックし、該当セクションを保守的な固定文に差し替えます。

これは文章の自然さよりも、数値の信頼性と誤解防止を優先するためです。

### PER計算でPDFのEPSを使わない理由

PERは「株価 ÷ EPS」で計算します。

EPSの期間や定義がズレると、PERも間違った数字になります。

そのためVer3.0では、PDF由来のEPSをPER計算に使いません。

ユーザー指定期間と一致したJ-Quants正式EPSが取得できた場合だけ、yfinanceの株価と組み合わせて試作用PERを計算します。

このPERは「現在株価ベースの実績PER」と呼びます。

株価は現在時点、EPSは指定決算期の実績値なので、「2025年3月期のPER」のように書くと誤解を招きます。

### J-Quants無料プランの注意点

調査の結果、J-Quants無料プランでは取得できるデータ期間に制限があることが分かりました。

最新の通期決算が企業PDFでは公開されていても、無料プランのAPIではまだ取得できない場合があります。

この場合、アプリは推測で補完せず「未取得」として扱います。
