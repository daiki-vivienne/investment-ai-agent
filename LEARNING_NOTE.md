# Learning Note

このノートは、3か月後の自分が読み返しても分かるように、アプリ全体の流れを初心者向けに整理したものです。

## システム全体の流れ

このアプリは、次の3ステップで動きます。

1. 決算PDFからテキストを取り出す
2. 取り出したテキストをAIに渡して投資家目線で分析してもらう
3. AIの分析結果をMarkdownファイルとして保存する

全体の流れは以下のイメージです。

```text
決算PDF
  ↓
pdf_reader.py でテキスト抽出
  ↓
ai_analyzer.py で投資家目線のAI分析
  ↓
report_writer.py でMarkdown保存
  ↓
data/reports/ に分析レポートができる
```

## 各ファイルの役割

### main.py

アプリの入り口です。

コマンドライン引数を受け取り、PDF読み込み、AI分析、Markdown保存を順番に実行します。

### src/config.py

`.env` から設定を読み込むファイルです。

主に `OPENAI_API_KEY` と `OPENAI_MODEL` を読み込みます。

APIキーをコードに直接書かないことで、秘密情報を守りやすくしています。

### src/pdf_reader.py

PDFからテキストを抽出するファイルです。

`pypdf` というライブラリを使って、PDFの各ページから文字を取り出します。

画像だけで作られたPDFの場合、文字を抽出できないことがあります。その場合は、将来的にOCRという画像文字認識の処理が必要になります。

### src/ai_analyzer.py

OpenAI APIを呼び出して、決算内容を投資家目線で分析するファイルです。

AIに対して「単なる要約ではなく、業績、リスク、半導体・AI観点、AIスコアまで見てほしい」と指示します。

### src/report_writer.py

AIが返したMarkdownテキストをファイルに保存するファイルです。

保存先フォルダが存在しない場合は、自動で作成します。

### data/pdfs/

分析したいPDFを置くフォルダです。

### data/reports/

作成されたMarkdownレポートが保存されるフォルダです。

## main.pyを実行した時の処理順序

たとえば、次のコマンドを実行したとします。

```powershell
python main.py data/pdfs/sample.pdf
```

このとき、処理は次の順番で進みます。

1. `main.py` が起動する
2. コマンドライン引数から `data/pdfs/sample.pdf` を受け取る
3. `config.py` が `.env` を読み込む
4. `pdf_reader.py` がPDFからテキストを抽出する
5. `ai_analyzer.py` が抽出テキストをAIに渡す
6. AIがMarkdown形式の投資分析レポートを返す
7. `report_writer.py` が `data/reports/` にMarkdownファイルを保存する
8. 保存先パスが画面に表示される

## PDF読み込みからMarkdown保存までの流れ

### 1. PDF読み込み

`extract_text_from_pdf()` がPDFファイルを開きます。

PDFの各ページを順番に読み、ページごとのテキストを取り出します。

### 2. AI分析

`analyze_financial_report()` がOpenAI APIを呼び出します。

その前に `build_analysis_prompt()` で、AIに渡す指示文を作ります。

この指示文には、以下の分析項目が含まれています。

- 会社概要
- 業績サマリー
- ポジティブ要因
- リスク要因
- 半導体・AI観点の分析
- 今後注目すべき指標
- 初心者向け解説
- 投資メモ
- AIスコア

AIスコアでは、成長性、収益性、財務健全性、将来性を0〜100点で出します。

### 3. Markdown保存

`save_markdown_report()` がAIの出力を `.md` ファイルとして保存します。

ファイル名には元PDFの名前と日時が入るため、後から見返しやすくなります。

## 初心者向けの補足

### なぜ投資家目線のプロンプトにしているのか

決算資料をただ要約するだけだと、「投資判断で何を見るべきか」が分かりにくくなります。

そのため、このアプリでは売上、利益、リスク、将来性、次回確認すべき指標まで整理してもらいます。

### なぜAIスコアを入れているのか

点数があると、複数企業を比較しやすくなります。

ただし、AIスコアは最終判断ではありません。資料から読み取れる範囲での目安として使います。

### なぜファイルを分けているのか

1つのファイルに全部書くと、最初は楽ですが、機能が増えたときに読みにくくなります。

このアプリでは、PDF読み込み、AI分析、保存処理を別々のファイルに分けています。

こうすることで、将来「ニュース取得だけ追加したい」「Discord通知だけ直したい」というときに、関係する場所を見つけやすくなります。

### なぜ.envを使うのか

OpenAI APIキーは、パスワードのような秘密情報です。

コードに直接書くと、GitHubに公開してしまう危険があります。

そのため、`.env` に書いて、Pythonコードから読み込む形にしています。

### なぜMarkdownで保存するのか

Markdownはテキスト形式なので、軽くて扱いやすいです。

GitHub、Notion、Obsidian、VS Codeなどでも読みやすく表示できます。

### このVer0.1でまだやらないこと

自動売買はまだ実装しません。

理由は、売買機能はお金に直接関わるため、PDF分析、スコアリング、ログ保存、失敗時の安全設計などを先に作る必要があるからです。

## Ver3.0で追加した考え方

Ver3.0では、PDFとAPIの期間が一致しているかを確認する仕組みを追加しました。

投資分析では、売上や利益の数字そのものだけでなく、「その数字がどの決算期のものか」がとても重要です。

たとえば、PDFが2026年3月期の通期決算なのに、APIから取れた数字が2026年3月期の3Q累計だった場合、その2つを同じものとして比較すると間違った分析になります。

### なぜPDFから決算期を推測しないのか

PDFには、通期実績、四半期実績、前年同期比、業績予想、脚注など、たくさんの数字が書かれています。

AIや単純な文字検索で決算期を推測すると、業績予想や脚注の数字を実績と勘違いする可能性があります。

そのため、Ver3.0ではPDFから決算期を推測しません。

ユーザーがCLIで指定した `--period-type` と `--fiscal-year-end` を正しい条件として扱います。

### Ver3.0の処理の流れ

```text
決算PDF
  ↓
PDFテキスト抽出
  ↓
ユーザー指定の決算期を確認
  ↓
J-Quantsで同じ期間のstatementを検索
  ↓
期間が一致した場合だけ正式データ候補にする
  ↓
期間が一致しないデータは参考データとして表示する
  ↓
AI分析
  ↓
期間整合性チェック付きMarkdownを保存
```

### 追加されたファイルの役割

### src/jquants_client.py

J-Quants APIから財務サマリーを取得し、ユーザー指定の決算期と一致するstatementを探します。

期間が一致しない場合は、正式データとして扱わず「未取得」にします。

### src/api_report_formatter.py

J-Quantsの確認結果をMarkdownに変換します。

「期間整合性チェック」「正式データ候補」「参考データ一覧」を作る役割です。

### なぜ参考データを分析に使わないのか

参考データは、存在を確認するためには便利です。

しかし、期間が違う数字を使って「利益が増えた」「PERが高い/低い」と判断すると、投資判断を誤る可能性があります。

そのため、Ver3.0では期間が一致しないAPIデータを分析やPER計算に使いません。

### なぜAIスコアも未取得にするのか

AIスコアは便利ですが、正式データがない状態で点数を出すと、PDF由来の参考数値を正式な根拠のように見せてしまう危険があります。

Ver3.0では、ユーザー指定期間と一致するAPI正式データが未取得の場合、AIスコアも「未取得」とします。

これは分析を弱くするためではなく、誤った自信を持たせないための安全設計です。

### なぜPDFの「過去最高」も分析根拠にしないのか

PDF上に「過去最高」や「大幅増加」と書かれていても、それがAPI正式データと同じ期間・同じ会計基準・同じ指標定義とは限りません。

そのためVer3.0では、API正式データが未取得の場合、PDF上の定量表現は「参考情報」として紹介するだけにします。

AI本文のポジティブ要因や投資メモでは、PDF由来の数値を根拠にした断定評価を避けます。

さらに、Ver3.0ではAPI正式データが未取得の場合、AIにPDF内の具体的な数値を書き写させません。

理由は、AIがPDF内の売上・営業利益・純利益・前年比の対応関係を読み間違える可能性があるためです。

数字を正確に扱う必要がある場合は、期間が一致したAPI正式データを使います。

### なぜ業績サマリーをAIに書かせないのか

AIに売上、営業利益、純利益、EPSを書かせると、PDF由来の数値や別定義の数値と混ざる可能性があります。

そのためVer3.0では、API正式業績サマリーをJ-Quants正式データからコードで機械的に生成します。

AI本文では、正式データの数値を再解釈させません。

### なぜAI出力後にも安全フィルタをかけるのか

プロンプトで注意しても、AIがPDF由来の「過去最高」「キャッシュフロー」「借入金」などを本文やスコア理由に混ぜてしまうことがあります。

そのためVer3.0では、AI出力後にも危険な表現をチェックし、該当セクションを保守的な固定文に差し替えます。

これは文章の自然さよりも、数値の信頼性と誤解防止を優先するためです。

### PER計算でPDFのEPSを使わない理由

PERは「株価 ÷ EPS」で計算します。

EPSの期間や定義がズレると、PERも間違った数字になります。

そのためVer3.0では、PDF由来のEPSをPER計算に使いません。

ユーザー指定期間と一致したJ-Quants正式EPSが取得できた場合だけ、yfinanceの株価と組み合わせて試作用PERを計算します。

このPERは「現在株価ベースの実績PER」と呼びます。

株価は現在時点、EPSは指定決算期の実績値なので、「2025年3月期のPER」のように書くと誤解を招きます。

### J-Quants無料プランの注意点

調査の結果、J-Quants無料プランでは取得できるデータ期間に制限があることが分かりました。

最新の通期決算が企業PDFでは公開されていても、無料プランのAPIではまだ取得できない場合があります。

この場合、アプリは推測で補完せず「未取得」として扱います。
